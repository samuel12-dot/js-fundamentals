/* Variables = Used for storing data
Things to note when working with data : 1. Keyword, 2. data_name, 3. assignment sign (=), 4. */
                     // KEYWORDS
/* Keywords are important words in javascript
Important keywords for declaring variables are:
1. let, 2. var, 3. const. */
                    //Scope
/* Scope
1. Global scope, 2. Local scope, 3. Block scope. */

const answer = 5 * 7 + 3

console.log(answer)